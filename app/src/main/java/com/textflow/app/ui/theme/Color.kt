package com.textflow.app.ui.theme

import androidx.compose.ui.graphics.Color

// ---------------------------------------------------------------------------
// TextFlow design tokens — spec §5 (canonical values, do not drift from these).
// ---------------------------------------------------------------------------

/** Near-black app background. */
val TextFlowBackground = Color(0xFF0E0D12)

/** Card / surface background. */
val TextFlowCard = Color(0xFF1C1B22)

/** Purple accent — command names, highlights. */
val TextFlowAccent = Color(0xFF8B7CF6)

/** Purple accent for filled toggles / buttons. */
val TextFlowAccentFill = Color(0xFF8A7CFB)

/** Gray secondary text / labels. */
val TextFlowSecondaryText = Color(0xFF9A98A5)

/** Primary text (white). */
val TextFlowOnPrimary = Color(0xFFFFFFFF)
